var _extends = Object.assign || function (target) { for (var i = 1; i < arguments.length; i++) { var source = arguments[i]; for (var key in source) { if (Object.prototype.hasOwnProperty.call(source, key)) { target[key] = source[key]; } } } return target; };

import React from 'react';
import PropTypes from 'prop-types';

var withA2HS = function withA2HS(Component) {
  var ChildComponent = function ChildComponent(props, context) {
    var deferredPrompt = context.deferredPrompt,
        isAppInstallable = context.isAppInstallable,
        isAppInstalled = context.isAppInstalled;


    return React.createElement(Component, _extends({
      deferredPrompt: deferredPrompt,
      isAppInstallable: isAppInstallable,
      isAppInstalled: isAppInstalled
    }, props));
  };

  ChildComponent.contextTypes = {
    deferredPrompt: PropTypes.any.isRequired,
    isAppInstallable: PropTypes.bool.isRequired,
    isAppInstalled: PropTypes.bool.isRequired
  };

  return ChildComponent;
};

export default withA2HS;