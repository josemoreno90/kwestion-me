'use strict';

exports.__esModule = true;
exports.logLoading = logLoading;
exports.clearLoading = clearLoading;
exports.clearAllLoadings = clearAllLoadings;

var _types = require('./types');

var types = _interopRequireWildcard(_types);

function _interopRequireWildcard(obj) { if (obj && obj.__esModule) { return obj; } else { var newObj = {}; if (obj != null) { for (var key in obj) { if (Object.prototype.hasOwnProperty.call(obj, key)) newObj[key] = obj[key]; } } newObj.default = obj; return newObj; } }

function logLoading(location) {
  return {
    type: types.LOG_LOADING,
    location: location
  };
}
function clearLoading(location) {
  return {
    type: types.CLEAR_LOADING,
    location: location
  };
}

function clearAllLoadings() {
  return {
    type: types.CLEAR_ALL_LOADINGS
  };
}